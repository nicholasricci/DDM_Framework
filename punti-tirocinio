-eliminato dal sort_matching_standard tutte le definizioni riferite a windows
-messa a punto dell'algoritmo interval_tree per far si che creasse una cartella bin durante il make
-creazione del file launcher.sh, compilazione di ogni algoritmo nella cartella Algorithms o pulizia con il comando clean della cartella Algorithms
-creazione del file configuration se non esiste e inizializzarlo. Se esiste possibile modificarlo tramite il comando configure
-messa a punto di launcher.sh con le funzioni e inserimento della funzione check_configure per controllare se il file configure esiste e contiene dei valori,
in caso non contenga valori viene richiamata la funzione configure.
-messa a punto dell'algoritmo interval_tree per far si che sia per brute_force che brute_force_mp creasse un file .txt separato l'uno dall'altro.
stessa cosa per interval_tree e interval_tree_mp.
-creazione di una libreria in c per l'input e output da utilizzare all'interno degli algoritmi inseriti dentro al framework. Basta copiare il 
file .c e .h e inserirli nel makefile ed utilizzare le funzioni all'interno per avere un input e un output simile per tutti gli algoritmi.
-utilizzo della libreria creata input output nei due algoritmi per renderli coerenti con il framework.
-durante il comando build per ogni algoritmo vengono creati due file: DDM_Parallel e DDM_Will_Execute.
DDM_Parallel: l'utilizzatore del framework dovrà scrivere all'interno il nome degli eseguibili degli algoritmi compilati con direttive parallele.
DDM_Will_Execute: l'utilizzatore del framework dovrà scrivere all'interno solo i nomi degli eseguibili che il framework dovrà eseguire.
-portato avanti il comando run, ora crea tutti i risultati e li mette nella cartella _result e _graph nella root del framework.
-fix comando run, sistemato il comando clean, si può ripulire sia i risultati che gli oggetti buildati.
-fix comando run, ora funziona per bene, anche le run, vengono create i file e spostati nella cartella _results e nella cartella _graphs.
-creato un file instruction.txt per sapere cosa fare.